
<% @images.each do |image| %>
  <h1><%= image.title %></h1>
  <p><%= link_to image_tag(image.attachment), image_path(image) %></p>
  <i><%= image.user.user_profile.username %></i>
<%end%>
<h1><%= link_to 'Add New Image', new_image_path %></h1>
